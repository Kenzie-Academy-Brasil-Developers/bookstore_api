import { Router } from "express";

export const booksRouter = Router();

booksRouter.post("/", )

booksRouter.get("/", )

booksRouter.get("/:id", )

booksRouter.patch("/:id", )

booksRouter.delete("/:id", )

